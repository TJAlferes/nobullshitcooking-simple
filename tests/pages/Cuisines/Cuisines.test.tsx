import { shallow } from 'enzyme';
import React from 'react';

import { Cuisines } from '../../../src/pages/Cuisines/Cuisines';

describe('Cuisines', () => {
  it('needs tests', () => {
    expect(1).toEqual(1);
  });
});