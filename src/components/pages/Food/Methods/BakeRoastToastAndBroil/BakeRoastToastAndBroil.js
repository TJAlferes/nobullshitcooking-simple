import React from 'react';

import '../method.css';

const BakeRoastToastAndBroil = props => (
  <div className={`method one-column-a ${props.oneColumnATheme}`}>
    <h1>Bake, Roast, Toast, and Broil</h1>
  </div>
);

export default BakeRoastToastAndBroil;