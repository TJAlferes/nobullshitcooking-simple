import React from 'react';

import './storage.css';

const Storage = props => (
  <div className={`storage one-column-a ${props.oneColumnATheme}`}>
    <h1>Storage</h1>
  </div>
);

export default Storage;