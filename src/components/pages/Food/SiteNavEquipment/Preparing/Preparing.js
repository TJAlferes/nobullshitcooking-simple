import React from 'react';

import './preparing.css';

const Preparing = props => (
  <div className={`preparing one-column-a ${props.oneColumnATheme}`}>
    <h1>Preparing</h1>
  </div>
);

export default Preparing;