import React from 'react';

import './cleaning.css';

const Cleaning = props => (
  <div className={`cleaning one-column-a ${props.oneColumnATheme}`}>
    <h1>Cleaning</h1>
  </div>
);

export default Cleaning;