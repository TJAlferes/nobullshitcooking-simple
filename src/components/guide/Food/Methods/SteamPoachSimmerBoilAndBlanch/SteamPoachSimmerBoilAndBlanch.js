import React from 'react';

import '../method.css';

const SteamPoachSimmerBoilAndBlanch = props => (
  <div className={`method one-column-a ${props.oneColumnATheme}`}>
    <h1>Steam, Poach, Simmer, Boil, and Blanch</h1>
  </div>
);

export default SteamPoachSimmerBoilAndBlanch;