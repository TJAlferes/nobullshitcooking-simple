import React from 'react';

import '../method.css';

const StewAndBraise = props => (
  <div className={`method one-column-a ${props.oneColumnATheme}`}>
    <h1>Stew and Braise</h1>
  </div>
);

export default StewAndBraise;